apiVersion: v1
kind: Service
metadata:
  name: fcg-catalog-api
spec:
  selector:
    app: fcg-catalog-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30002

  type: NodePort
